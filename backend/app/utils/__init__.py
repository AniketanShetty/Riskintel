"""
utils/

Shared utility modules — activated in Phase 2:

    preprocess_a.py  → Clean loan_approval_dataset.csv → eligibility_data.csv
    preprocess_b.py  → Clean RuralCreditData.csv → readiness_data.csv + livelihood_data.csv
    preprocess_c.py  → Clean External_Cibil_Dataset.csv → risk_tier_thresholds.json + archetype_data.csv
    validators.py    → Request body validation helpers (Phase 9)
"""
