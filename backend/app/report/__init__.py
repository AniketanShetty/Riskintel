"""
report/

PDF report generation for the frozen /api/report/* endpoints.

The HTTP surface is implemented in app/api/reports.py. The PDF rendering
helper lives there too. This package is reserved for future layout/style
modules (Person A/B section renderers, design system).
"""
